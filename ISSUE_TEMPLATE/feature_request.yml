name: Feature request
description: Propose a small, focused improvement
labels: [feature]
body:
  - type: textarea
    id: pitch
    attributes: { label: The pitch (1–3 sentences) }
    validations: { required: true }
  - type: textarea
    id: details
    attributes: { label: Details / constraints }
  - type: checkboxes
    id: scope
    attributes:
      label: Scope
      options:
        - label: Small and focused (single concern)
          required: true
